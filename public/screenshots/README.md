# TODO: Drop real product screenshots here (customer-provided).
# Do NOT use stock "dashboard" images or fabricated UI captures.
# Hero CSS mockup is allowed with the "Representative view" / "Temsili görünüm" note.
# When files arrive, reference them from feature sections — never invent product UI photos.
